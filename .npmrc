# Expo + React Native pin exact transitive versions that npm's strict peer
# resolution rejects in a mixed web/native monorepo. This is Expo's documented
# recommendation for npm. https://docs.expo.dev/guides/monorepos/
legacy-peer-deps=true
